PAYMENT_SECRET_KEY=sk_liv...TPs5/9+VvTQciJT96VQgWhauvkixValbeotokJIgeGP0su5+ulHRoDo0DqOlPK+EA==
PORT=3000

# ⚠️ ADMIN PIN — substitua imediatamente por um PIN seguro auto-gerado
# Recomendação: rode `node -e "console.log(require('crypto').randomInt(0,1000000).toString().padStart(6,'0'))"`
# e cole o resultado abaixo. Não use 194521 (pwnável).
ADMIN_PIN=830927
ADMIN_TOKEN=replace_this_with_a_long_random_bearer_token
